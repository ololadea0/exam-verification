services:
  - type: web
    name: exam-verification
    runtime: node
    rootDir: .
    buildCommand: npm run install:all && npm run build
    startCommand: npm run start

    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
